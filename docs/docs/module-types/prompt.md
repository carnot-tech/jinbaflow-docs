# Prompt

Sends a prompt to an AI and returns its response.

## Inputs

You can define your own variables to use in the code with `context.{var_name}` as your inputs.

## Outputs

`result`: `[String]` The response from the AI.